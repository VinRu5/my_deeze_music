import 'package:flutter/material.dart';

class PodcastDetails extends StatelessWidget {
  static const route = '/podcast/details';
  const PodcastDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
